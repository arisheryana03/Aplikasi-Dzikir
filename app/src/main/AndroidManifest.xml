<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aries.dzikirpagipetang">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MenuItem.SettingActivity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".MenuItem.AboutActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".Activity.DzikirSholatActivity"
            android:theme="@style/AppThemeDzikir"/>
        <activity
            android:name=".Activity.SoreActivity"
            android:theme="@style/AppThemePetang" />
        <activity
            android:name=".Activity.PagiActivity"
            android:theme="@style/AppThemePagi" />
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SplashScreen.SplashActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>